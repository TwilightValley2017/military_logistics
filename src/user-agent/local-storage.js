tip('local-storage loaded')

const KEYS = {
    MONDAY: 'MONDAY',
    TUESDAY: 'TUESDAY',
}

localStorage.setItem(KEYS.MONDAY, 'Today is Monday!')
localStorage.setItem(KEYS.TUESDAY, 'Today is Tuesday!')